# Repository instructions

- After completing any requested code or documentation change, run the relevant tests, commit the intended changes, and push the current branch to the `origin` remote.
- Do not commit secrets. Keep `.env` and any other local credential files ignored.
- If a push cannot be completed because authentication, permissions, or the remote is unavailable, report that clearly instead of treating the task as finished.
